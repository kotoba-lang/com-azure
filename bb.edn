{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([azure.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'azure.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
